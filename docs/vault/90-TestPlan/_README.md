# 90-TestPlan — Test plans

- `TP-<slug>.md` — test plan ต่อ feature (scenarios + done definition)
- text link-note ของผล run (report) ชี้ไป evidence manifest

Evidence จริง (screenshots, logs, manifest) อยู่ **นอก vault** ใต้
`test-artifacts/<date>/<source>-<NN>-<slug>/` (gitignored) — captured by /ow-test
