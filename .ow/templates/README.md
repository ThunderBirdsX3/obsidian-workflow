<!--
═══════════════════════════════════════════════════════════════════════════════
  READ-ONLY · DO NOT EDIT THIS FILE
═══════════════════════════════════════════════════════════════════════════════
  Files under `.ow/templates/` are the shipped obsidian-workflow snapshot
  and the **whole folder is overwritten** when `/ow-sync` runs
  ────────────────────────────────────────────────────────────────────────────
  Need to change something?
    • Project-wide (commit + share with the team) → copy this file to `templates/<name>.md`
      → edit it there → commit  (lookup priority higher than .ow/templates/)
    • Personal-only (not committed)               → copy to `.ow/local/templates/<name>.md`
      → edit it there (gitignored)                (highest lookup priority)
  ════════════════════════════════════════════════════════════════════════════
-->

# `.ow/templates/` — shipped template defaults

The templates here are the canonical set every project using obsidian-workflow starts from

Template lookup order:
1. `.ow/local/templates/<name>.md` — personal override (gitignored)
2. `templates/<name>.md` — project override (git-tracked)
3. `.ow/templates/<name>.md` — shipped default (fallback)

The highest-priority file that exists wins (a project can adapt a template to its own context)

## Never

- Never edit a file under `.ow/templates/` directly inside a project — use `templates/` instead
- Changes to the shipped set go through the obsidian-workflow repo + `/ow-sync`

## Files that should be here

(the shipped set — a project overrides one by copying it to `templates/<name>.md` and editing it there)

- adr.md
- checklist.md
- design-component.md
- feature.md
- fix-log.md
- flow.md
- function.md
- handoff.md
- phase.md
- plan.md
- prd.md
- role.md
- srs.md
- tech-spec.md
- test-plan.md
- test-scenario-report.md

## Note

In a clean obsidian-workflow install, only `.ow/templates/` is populated; root `templates/` stays empty until a project overrides something. Editing `templates/<name>.md` overrides that one template for that one project
