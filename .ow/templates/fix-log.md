<!--
═══════════════════════════════════════════════════════════════════════════════
  READ-ONLY · DO NOT EDIT THIS FILE
═══════════════════════════════════════════════════════════════════════════════
  Files under `.ow/templates/` are the shipped obsidian-workflow snapshot
  and the **whole folder is overwritten** when `/ow-sync` runs
  ────────────────────────────────────────────────────────────────────────────
  Need to change something?
    • Project-wide (commit + share with the team) → copy this file to `templates/<name>.md`
      → edit it there → commit  (lookup priority higher than .ow/templates/)
    • Personal-only (not committed)               → copy to `.ow/local/templates/<name>.md`
      → edit it there (gitignored)                (highest lookup priority)
  ════════════════════════════════════════════════════════════════════════════
-->

---
tags: [type/fix-log]
date: <YYYY-MM-DD HH:mm>
title: <one-line bug title>
status: in-progress           # in-progress | fixed | wont-fix | regressed
severity: P1                   # P0 blocker | P1 major | P2 minor | P3 polish
area: <api | web | mobile | cross>
reported_by: <user | qa | self | customer>
related_plan: <"[[plan-slug]]" or none>   # /ow-fix→/ow-plan path: /ow-plan fix: writes the back-link; closed automatically by /ow-implement (#30)
# fixed_commit / fixed_in_version — filled in automatically on close (/ow-fix path: /ow-git --bump · /ow-fix-issue path: closes + stamps itself in Phase 8.5)
---

# <Bug title>

## Symptom
<what user sees / what's broken>

## Reproduction
1. <step 1>
2. <step 2>
3. Expected: <X>; Actual: <Y>

## Root Cause
<technical cause — file path, function, line — from grep/read>

## Vault Context Read
- <[[FN-related]]>
- <[[REF-AuthorizationMatrix]] if auth-related>

## RED baseline
- Reproduce test: <test-cmd> → FAIL / EXIT≠0 — or `pending verification`
```
<the failing output, verbatim + trimmed>
```
- Observed wrong state (UI bug): <what is on screen vs what should be> — or N/A

## Fix Approach
<a paragraph describing the fix to be made — not code yet>

## Affected Files
- `path/to/file1.ts` — <what to change>

## Test Cases (to prove the fix works)
- [ ] Test 1: <scenario>
- [ ] Regression test: <scenario>

## Risk
- <risk> → mitigation: <plan>

## Next
- [ ] run `/ow-plan fix:<slug>` — creates the plan + wires the two-way link (`source_fix:` ↔ `related_plan:`)
- [ ] run `/ow-implement` to apply the actual fix (prove red→green with a real run)
- [ ] close the fix-log: `status: fixed` + tick the checkboxes — 🤖 automatic via `/ow-implement` when the plan is done; `fixed_in_version`/`fixed_commit` via `/ow-git --bump` (#30)

---

## GREEN result (filled after fix)
- Same test → PASS
```
<the passing output, verbatim + trimmed>
```
- Observed fixed state (UI bug): <what is on screen now> — or N/A
- Test results: <pass/fail count>
- Commit: <hash>
- Status: <fixed | regressed | wont-fix>
