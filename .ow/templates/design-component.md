<!--
═══════════════════════════════════════════════════════════════════════════════
  READ-ONLY · DO NOT EDIT THIS FILE
═══════════════════════════════════════════════════════════════════════════════
  Files under `.ow/templates/` are the shipped obsidian-workflow snapshot
  and the **whole folder is overwritten** when `/ow-sync` runs
  ────────────────────────────────────────────────────────────────────────────
  Need to change something?
    • Project-wide (commit + share with the team) → copy this file to `templates/<name>.md`
      → edit it there → commit  (lookup priority higher than .ow/templates/)
    • Personal-only (not committed)               → copy to `.ow/local/templates/<name>.md`
      → edit it there (gitignored)                (highest lookup priority)
  ════════════════════════════════════════════════════════════════════════════
-->

## <ComponentName>

### Purpose
<when to use, when not — concrete>

### Anatomy
- Container
- Slot/children
- Icon (optional)
- <other parts>

### Variants
- **size**: sm / md / lg
- **intent**: primary / secondary / danger / ghost
- **state**: default / hover / focus / disabled / loading / error

### Props (framework-agnostic)
| Prop | Type | Default | Note |
|---|---|---|---|
| ... | ... | ... | ... |

### Accessibility
- Role: <button | link | textbox | ...>
- ARIA: <attributes>
- Keyboard: <Enter / Space / arrows>
- Focus visible: <how>
- Screen reader: <expected announcement>

### Tokens Used
- color: `--color-primary-500`, `--color-text-on-primary`
- spacing: `--space-3`, `--space-4`
- radius: `--radius-md`
- typography: `--text-body-md`
- shadow: `--shadow-sm`
- motion: `--motion-fast`

### Example (framework-aware)
```tsx
<Button intent="primary" size="md" onClick={save}>
  Save
</Button>
```

### Don't
- Never put in more than 1 icon
- Never use it in a dense table cell (use the ghost variant)
- Never change the color by overriding the class
